# Public Supabase config. These are already hardcoded as defaults in
# src/lib/supabase.ts (safe to ship — protected by RLS). Set them here only if
# you rotate the project or key.
VITE_SUPABASE_URL=https://cxexzksxgpuiuqmrkjce.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_R8lkd84h8-a6UBkWSdEmgw_XCP2gG8k

# SECRET — required ONLY for the in-app /admin/waitlist CSV export.
# Get it from Supabase: Project Settings -> API -> service_role. Never commit it.
SUPABASE_SERVICE_ROLE_KEY=

# Optional — require this key on the /admin/waitlist export page.
WAITLIST_KEY=
